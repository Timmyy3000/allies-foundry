diff --git a/gateway/platforms/api_server.py b/gateway/platforms/api_server.py
index 7a9a149fe..e0fd39f35 100644
--- a/gateway/platforms/api_server.py
+++ b/gateway/platforms/api_server.py
@@ -2576,6 +2576,7 @@ class APIServerAdapter(BasePlatformAdapter):
         session_model: Optional[str] = None,
         confirmed_runtime_lock: bool = False,
         routine_result: bool = False,
+        routine_tool_context: tuple | None = None,
     ) -> Any:
         """
         Create an AIAgent instance using the gateway's runtime config.
@@ -2854,6 +2855,11 @@ class APIServerAdapter(BasePlatformAdapter):
         disabled_toolsets = (
             [] if routine_result else [_ALLIES_ROUTINE_RESULT_TOOLSET]
         )
+        disabled_toolsets.append("cronjob")
+        if routine_tool_context is not None and not routine_result:
+            enabled_toolsets.append("allies-routines")
+        else:
+            disabled_toolsets.append("allies-routines")
         max_iterations = _current_max_iterations()
 
         # Load fallback provider chain so the API server platform has the
@@ -3828,6 +3834,17 @@ class APIServerAdapter(BasePlatformAdapter):
         )
         if routine_result_requested:
             system_prompt = _ALLIES_ROUTINE_RESULT_SYSTEM_PROMPT
+        routine_tool_context = None
+        if not routine_result_requested and request.headers.get("X-Allies-Routine-Tool"):
+            from tools.allies_routines import INSTRUCTION, turn_context
+            try:
+                routine_tool_context = turn_context(
+                    request.headers["X-Allies-Routine-Tool"],
+                    request.headers.get("X-Allies-Foundry-Origin", ""),
+                )
+            except ValueError:
+                return web.json_response({"error": "invalid routine context"}, status=400)
+            system_prompt = (system_prompt or "") + "\n" + INSTRUCTION + "\nCurrent time: " + datetime.now().astimezone().isoformat()
         # Runtime selection — mirrors _handle_session_chat (lock wins,
         # otherwise session-persisted model then per-request values).
         runtime_request = self._effective_session_runtime_request(
@@ -4034,6 +4051,7 @@ class APIServerAdapter(BasePlatformAdapter):
                     agent_ref=agent_ref,
                     approval_context=approval_context,
                     routine_result=routine_result_requested,
+                    routine_tool_context=routine_tool_context,
                     **agent_overrides,
                 )
                 final_response = _resolve_media_to_data_urls(result.get("final_response", "") if isinstance(result, dict) else "")
@@ -6467,6 +6485,7 @@ class APIServerAdapter(BasePlatformAdapter):
         confirmed_runtime_lock: bool = False,
         approval_context: Optional[Dict[str, Any]] = None,
         routine_result: bool = False,
+        routine_tool_context: tuple | None = None,
     ) -> tuple:
         """
         Create an agent and run a conversation in a thread executor.
@@ -6509,6 +6528,8 @@ class APIServerAdapter(BasePlatformAdapter):
                     session_id=session_id or "",
                 )
                 approval_context_token = None
+                from tools.allies_routines import context as routine_context
+                routine_context_token = routine_context.set(routine_tool_context)
                 approval_session_token = None
                 agent = None
                 try:
@@ -6544,6 +6565,7 @@ class APIServerAdapter(BasePlatformAdapter):
                         session_model=session_model,
                         confirmed_runtime_lock=confirmed_runtime_lock,
                         routine_result=routine_result,
+                        routine_tool_context=routine_tool_context,
                     )
                     if agent_ref is not None:
                         agent_ref[0] = agent
@@ -6694,6 +6716,7 @@ class APIServerAdapter(BasePlatformAdapter):
                         except Exception:
                             pass
                     clear_session_vars(tokens)
+                    routine_context.reset(routine_context_token)
 
         self._activate_admitted_request()
         self._inflight_agent_runs += 1
