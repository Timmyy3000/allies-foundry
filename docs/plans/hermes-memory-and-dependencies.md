# Hermes memory and skill prerequisites

Fast route. A separate Foundry PR enables the existing reviewed Mnemosyne tools for managed Allies and fixes the missing xurl executable. No UI work, new service, or deployment is included.

Use the existing profile seed, fingerprint and reconciliation paths. Upgrade the exact former managed context-only policy to narrow tools without replacing sessions, memories or custom settings. Legacy records do not retain whether the identical default was explicitly selected; the user's all-Allies policy applies to that exact tuple. Preserve nondefault configurations and reject ambiguous filesystem state. Keep profile isolation, schema validation and retention checks. Clarify memory instructions for normal and routine conversations.

Install a pinned, checksum-verified official xurl release in the image, with its license. Keep other optional dependencies task-driven through existing terminal/package tooling and persistent storage; do not install every skill's dependencies or add an installer service. Document executable, account, platform and external-service prerequisites separately. Audit the actual pinned image and configured toolsets for additional limitations.

Validation: existing backend/runtime policy and reconciliation tests, cross-session memory smoke through the real provider, image build and xurl help under runtime UID, existing skills smoke and focused catalog tests. Run repository lint/checks and relevant full suites before delivery, with separate correctness and simplicity review. Existing targets: make validate, make runtime-test, make hermes-image-test. Record concrete invocation/results in the PR.

Results: final Linux runtime suite 629 passed/2 skipped, but coverage is 89.54%, below the existing 90% gate (the base has the same coverage shortfall; routines PR #56 carries the coverage fixes). Backend full run had 644 passes/13 skips and two failures: a historical receipt golden value changed with the intended new seed default, and a local CRLF checkout changed locked artifact bytes. The receipt assertion now checks the new fixed digest plus persisted identity; restoring artifact HEAD bytes and rerunning both affected suites passed all 39 tests. Provider tests: 21 passed. Migration probes: 3 passed. Image builds; runtime-user xurl, native skills across container restart, and memory recall across provider restart/session change pass. Separate independent correctness and simplicity reviews have no remaining actionable findings. Hosted readiness must be rechecked after the base coverage fix; do not lower the gate.

Rollback: revert the code and publish the previous image pair, which already supports explicit narrow-tools profiles. The data migration is forward-only and leaves upgraded memory policy and profile data intact; reverting code does not disable the tools again. A deliberate policy change is needed to disable them. Do not claim authenticated X access from an executable smoke test. Staging adoption and account connection remain separate from local validation.
